rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /payment_proofs/{allPaths=**} {
      allow read, write: if true; // Public upload for payment screenshots
    }
  }
}
